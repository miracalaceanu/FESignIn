# Skip optional dependencies to avoid node-sass issues
optional=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Suppress warnings for cleaner output
loglevel=warn

# Set Python path if needed (uncomment and adjust if you have Python 2.7)
# python=C:\Python27\python.exe